# نظام الصفوف الدراسية

تم إضافة نظام موحد للصفوف الدراسية في جميع أنحاء التطبيق! ✅

## 📚 الصفوف المتاحة

تم تعريف جميع الصفوف الدراسية المصرية في ملف ثابت واحد لسهولة الإدارة والتحديث.

### المرحلة الابتدائية
1. الأول الابتدائي
2. الثاني الابتدائي
3. الثالث الابتدائي
4. الرابع الابتدائي
5. الخامس الابتدائي
6. السادس الابتدائي

### المرحلة الإعدادية
7. الأول الإعدادي
8. الثاني الإعدادي
9. الثالث الإعدادي

### المرحلة الثانوية
10. الأول الثانوي
11. الثاني الثانوي
12. الثالث الثانوي

---

## 📁 الملف المصدري

**المسار:** `src/lib/constants.ts`

```typescript
export const CLASSES = [
  // المرحلة الابتدائية
  'الأول الابتدائي',
  'الثاني الابتدائي',
  'الثالث الابتدائي',
  'الرابع الابتدائي',
  'الخامس الابتدائي',
  'السادس الابتدائي',
  
  // المرحلة الإعدادية
  'الأول الإعدادي',
  'الثاني الإعدادي',
  'الثالث الإعدادي',
  
  // المرحلة الثانوية
  'الأول الثانوي',
  'الثاني الثانوي',
  'الثالث الثانوي',
] as const;

export type ClassName = typeof CLASSES[number];
```

---

## ✅ الصفحات المحدثة

### 1. إضافة مستخدم جديد (سوبر أدمن)
**المسار:** `/dashboard/super-admin/users/new`

- يظهر حقل "الصف الدراسي" فقط عند اختيار دور "طالب"
- قائمة منسدلة بجميع الصفوف المتاحة
- الصف الافتراضي: "الأول الابتدائي"

### 2. إدارة الطلاب (سوبر أدمن)
**المسار:** `/dashboard/super-admin/students`

- فلترة الطلاب حسب الصف
- قائمة منسدلة بجميع الصفوف + "جميع الصفوف"
- تعرض جميع الصفوف حتى لو لم يكن فيها طلاب

### 3. طلاب المعلم
**المسار:** `/dashboard/teacher/students`

- فلترة الطلاب حسب الصف
- قائمة منسدلة بجميع الصفوف + "جميع الصفوف"
- تعرض جميع الصفوف المعرفة في النظام

---

## 🎯 المميزات

### 1. مصدر واحد للحقيقة (Single Source of Truth)
- جميع الصفوف معرفة في مكان واحد
- سهولة التحديث والصيانة
- تجنب التكرار والأخطاء

### 2. النوع الآمن (Type-Safe)
```typescript
export type ClassName = typeof CLASSES[number];
```
- TypeScript يتحقق من صحة أسماء الصفوف
- منع الأخطاء الإملائية

### 3. سهولة الاستخدام
```typescript
import { CLASSES } from '@/lib/constants';

// في أي مكون
<select>
  {CLASSES.map(className => (
    <option key={className} value={className}>
      {className}
    </option>
  ))}
</select>
```

### 4. الاتساق
- نفس القائمة في جميع أنحاء التطبيق
- نفس الترتيب والتنسيق
- تجربة مستخدم متسقة

---

## 📝 كيفية الاستخدام

### في مكون React
```typescript
import { CLASSES } from '@/lib/constants';

export default function MyComponent() {
  const [selectedClass, setSelectedClass] = useState(CLASSES[0]);
  
  return (
    <select value={selectedClass} onChange={(e) => setSelectedClass(e.target.value)}>
      {CLASSES.map(className => (
        <option key={className} value={className}>
          {className}
        </option>
      ))}
    </select>
  );
}
```

### في الفلترة
```typescript
import { CLASSES } from '@/lib/constants';

// فلترة حسب جميع الصفوف
<select>
  <option value="all">جميع الصفوف</option>
  {CLASSES.map(className => (
    <option key={className} value={className}>
      {className}
    </option>
  ))}
</select>
```

### في النماذج
```typescript
// لطالب جديد
const [formData, setFormData] = useState({
  name: '',
  class: CLASSES[0], // الصف الافتراضي
  // ...
});
```

---

## 🔄 التحديثات المستقبلية

### إضافة صفوف جديدة
ببساطة قم بتعديل ملف `src/lib/constants.ts`:

```typescript
export const CLASSES = [
  // المراحل الموجودة...
  
  // مرحلة جديدة
  'الصف الجديد',
] as const;
```

جميع الصفحات ستحدث تلقائياً!

### تصنيف حسب المرحلة
يمكن إضافة تصنيف إضافي:

```typescript
export const CLASSES_BY_STAGE = {
  'ابتدائي': [
    'الأول الابتدائي',
    'الثاني الابتدائي',
    // ...
  ],
  'إعدادي': [
    'الأول الإعدادي',
    // ...
  ],
  'ثانوي': [
    'الأول الثانوي',
    // ...
  ],
};
```

---

## 🎨 التصميم

### قائمة منسدلة جميلة
```tsx
<select className="input-field">
  <option value="all">جميع الصفوف</option>
  {CLASSES.map(className => (
    <option key={className} value={className}>
      {className}
    </option>
  ))}
</select>
```

### شارات الصفوف (Badges)
```tsx
<span className="badge-primary">{student.class}</span>
```

---

## ✨ الفوائد

### للمطورين
- ✅ كود نظيف وقابل للصيانة
- ✅ تجنب الأخطاء الإملائية
- ✅ Type-safe مع TypeScript
- ✅ سهولة الاختبار

### للمستخدمين
- ✅ قوائم منظمة ومرتبة
- ✅ لا أخطاء في كتابة اسم الصف
- ✅ تجربة متسقة في كل مكان
- ✅ سهولة الاختيار

### للنظام
- ✅ بيانات موحدة
- ✅ سهولة البحث والفلترة
- ✅ تقارير دقيقة
- ✅ إحصائيات صحيحة

---

## 📊 أمثلة على الاستخدام

### 1. إحصائيات الصفوف
```typescript
const getStudentsByClass = () => {
  const result: Record<string, number> = {};
  CLASSES.forEach(className => {
    result[className] = students.filter(s => s.class === className).length;
  });
  return result;
};
```

### 2. تقرير شامل
```typescript
CLASSES.forEach(className => {
  const classStudents = students.filter(s => s.class === className);
  console.log(`${className}: ${classStudents.length} طالب`);
});
```

### 3. التحقق من الصحة
```typescript
if (!CLASSES.includes(studentClass)) {
  throw new Error('صف دراسي غير صحيح');
}
```

---

## 🔍 البحث والفلترة

الآن يمكن البحث والفلترة بشكل دقيق:

```typescript
// الفلترة حسب الصف
const filteredStudents = students.filter(student => 
  classFilter === 'all' || student.class === classFilter
);

// البحث في اسم الصف
const searchResults = students.filter(student =>
  student.class.includes(searchTerm)
);
```

---

تم تطبيق نظام موحد للصفوف الدراسية في جميع أنحاء التطبيق! 🎓
