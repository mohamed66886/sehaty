# حصتي - Hesaty Platform

## متابعة الطالب لحظة بلحظة

منصة تعليمية متكاملة لإدارة المراكز التعليمية مع نظام متقدم لمتابعة الطلاب والمعلمين وأولياء الأمور.

---

## 🚀 المميزات

### للمعلمين
- ✅ متابعة الطلاب المسجلين
- ✅ تسجيل الحضور والغياب
- ✅ إضافة وإدارة الواجبات المنزلية
- ✅ إنشاء وإدارة الامتحانات
- ✅ تصحيح الامتحانات ورصد الدرجات
- ✅ عرض إحصائيات الطلاب

### للطلاب
- ✅ عرض المعلمين المسجلين
- ✅ متابعة سجل الحضور
- ✅ عرض الواجبات المطلوبة والمواعيد النهائية
- ✅ حل الامتحانات الإلكترونية
- ✅ عرض النتائج والدرجات
- ✅ متابعة الأداء الدراسي

### لأولياء الأمور
- ✅ متابعة أبنائهم المسجلين
- ✅ عرض سجل الحضور والغياب
- ✅ متابعة النتائج والدرجات
- ✅ تلقي إشعارات عن أداء أبنائهم
- ✅ التواصل مع المعلمين

### للإدارة (Super Admin)
- ✅ إدارة جميع المستخدمين
- ✅ إنشاء وإدارة المراكز التعليمية
- ✅ عرض إحصائيات شاملة للنظام
- ✅ إدارة الصلاحيات
- ✅ مراقبة الأنشطة

---

## 🛠️ التقنيات المستخدمة

- **Frontend Framework:** Next.js 14 (App Router)
- **Language:** TypeScript
- **Styling:** Tailwind CSS
- **Backend:** Firebase
  - Authentication
  - Firestore Database
  - Storage
  - Analytics
- **UI Components:** Lucide React Icons
- **Forms:** React Hook Form + Zod
- **RTL Support:** مدمج بالكامل

---

## 📋 المتطلبات

- Node.js 18.x أو أحدث
- npm أو yarn
- حساب Firebase

---

## ⚙️ التثبيت والإعداد

### 1. تثبيت المتطلبات

```bash
npm install
```

### 2. إعداد Firebase

#### أ. إنشاء مشروع Firebase

1. اذهب إلى [Firebase Console](https://console.firebase.google.com/)
2. أنشئ مشروع جديد
3. فعّل Firebase Authentication (Email/Password)
4. أنشئ قاعدة بيانات Firestore
5. أنشئ Firebase Storage

#### ب. الحصول على بيانات الاعتماد

1. في إعدادات المشروع، اذهب إلى "Your apps"
2. أنشئ تطبيق ويب جديد
3. انسخ بيانات الإعداد (Firebase Config)

#### ج. إعداد ملف البيئة

انسخ ملف `.env.example` إلى `.env.local`:

```bash
cp .env.example .env.local
```

املأ بيانات Firebase في `.env.local`:

```env
NEXT_PUBLIC_FIREBASE_API_KEY=your_api_key_here
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_project_id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your_project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
NEXT_PUBLIC_FIREBASE_APP_ID=your_app_id
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=your_measurement_id
```

### 3. رفع قواعد الأمان إلى Firestore

```bash
# تثبيت Firebase CLI
npm install -g firebase-tools

# تسجيل الدخول
firebase login

# تهيئة المشروع
firebase init firestore

# رفع القواعد
firebase deploy --only firestore:rules
```

### 4. تشغيل المشروع

```bash
npm run dev
```

افتح المتصفح على [http://localhost:3000](http://localhost:3000)

---

## 📊 هيكل قاعدة البيانات

### Collections

#### 1. users
```typescript
{
  uid: string,
  name: string,
  role: 'super_admin' | 'teacher' | 'student' | 'parent',
  phone: string,
  email: string,
  createdAt: Timestamp
}
```

#### 2. teachers
```typescript
{
  uid: string,
  subjects: string[],
  centerId: string,
  studentIds: string[], // IDs of linked students
  createdAt: Timestamp
}
```

#### 3. students
```typescript
{
  uid: string,
  centerId: string,
  parentId: string,      // REQUIRED
  teacherIds: string[],  // REQUIRED (one or more)
  class: string,
  createdAt: Timestamp
}
```

#### 4. parents
```typescript
{
  uid: string,
  studentIds: string[], // IDs of their children
  createdAt: Timestamp
}
```

#### 5. attendance
```typescript
{
  studentId: string,
  teacherId: string,
  date: Timestamp,
  status: 'present' | 'absent' | 'late' | 'excused',
  notes?: string,
  createdAt: Timestamp
}
```

#### 6. homework
```typescript
{
  teacherId: string,
  studentIds: string[],
  title: string,
  description: string,
  deadline: Timestamp,
  subject: string,
  createdAt: Timestamp
}
```

#### 7. exams
```typescript
{
  teacherId: string,
  studentIds: string[],
  title: string,
  subject: string,
  questions: Question[],
  totalScore: number,
  duration: number,
  startDate: Timestamp,
  endDate: Timestamp,
  createdAt: Timestamp
}
```

#### 8. results
```typescript
{
  studentId: string,
  examId: string,
  score: number,
  answers: Record<string, string>,
  submittedAt: Timestamp,
  gradedAt?: Timestamp,
  feedback?: string
}
```

---

## 🔐 قواعد الأمان والعلاقات

### قواعد العلاقات الإجبارية

1. **كل طالب يجب أن يكون مرتبطاً بـ:**
   - ولي أمر واحد (إجباري)
   - معلم واحد أو أكثر (إجباري)

2. **كل ولي أمر:**
   - يمكن أن يكون لديه طالب واحد أو أكثر

3. **كل معلم:**
   - يمكن أن يكون لديه طلاب متعددين

### الصلاحيات

#### Super Admin
- الوصول الكامل لجميع البيانات
- إنشاء وتعديل وحذف المستخدمين
- إدارة المراكز التعليمية

#### Teacher
- قراءة بيانات الطلاب المرتبطين فقط
- إنشاء وتعديل الحضور للطلاب المرتبطين
- إنشاء وتعديل الواجبات والامتحانات
- تصحيح الامتحانات

#### Student
- قراءة البيانات الخاصة فقط
- عرض المعلمين المرتبطين
- حل الامتحانات
- عرض النتائج والواجبات

#### Parent
- قراءة بيانات أبنائهم فقط
- عرض الحضور والنتائج
- لا يمكنهم التعديل

---

## 🎨 الواجهة والتصميم

### الألوان الرئيسية
- Primary: Blue (#0ea5e9)
- Success: Green (#10b981)
- Warning: Yellow (#f59e0b)
- Danger: Red (#ef4444)

### الخطوط
- خط Cairo للنصوص العربية
- دعم كامل لـ RTL

### المكونات
- Cards
- Buttons (Primary, Secondary, Danger)
- Input Fields
- Badges (Success, Warning, Danger, Info)
- Tables
- Loading States

---

## 📱 الصفحات والمسارات

### صفحات عامة
- `/` - الصفحة الرئيسية
- `/login` - تسجيل الدخول
- `/register` - إنشاء حساب (إذا كان مسموحاً)

### لوحات التحكم
- `/dashboard/super-admin` - لوحة تحكم المسؤول
- `/dashboard/teacher` - لوحة تحكم المعلم
- `/dashboard/student` - لوحة تحكم الطالب
- `/dashboard/parent` - لوحة تحكم ولي الأمر

---

## 🔄 سير العمل

### إضافة طالب جديد

1. Super Admin ينشئ حساب ولي أمر
2. Super Admin ينشئ حساب معلم (أو يستخدم معلم موجود)
3. Super Admin ينشئ حساب طالب ويربطه بـ:
   - ولي الأمر (إجباري)
   - معلم واحد أو أكثر (إجباري)
4. يتم تحديث العلاقات تلقائياً في قاعدة البيانات

### تسجيل الحضور

1. المعلم يدخل إلى لوحة التحكم
2. يختار قسم "الحضور"
3. يختار التاريخ
4. يسجل حضور/غياب كل طالب من طلابه المرتبطين
5. يحفظ البيانات

### إضافة واجب

1. المعلم يدخل إلى "الواجبات"
2. ينشئ واجب جديد
3. يحدد الطلاب المطلوب منهم
4. يحدد الموعد النهائي
5. يحفظ الواجب

### متابعة ولي الأمر

1. ولي الأمر يدخل إلى لوحة التحكم
2. يرى قائمة أبنائه
3. يختار الابن للمتابعة
4. يعرض:
   - سجل الحضور
   - الواجبات
   - النتائج
   - الإحصائيات

---

## 🚀 البناء والنشر

### بناء المشروع للإنتاج

```bash
npm run build
```

### تشغيل النسخة الإنتاجية محلياً

```bash
npm run start
```

### النشر

يمكن نشر المشروع على:

#### Vercel (موصى به)
```bash
npm install -g vercel
vercel
```

#### Firebase Hosting
```bash
npm run build
firebase deploy --only hosting
```

#### أي خدمة استضافة تدعم Next.js

---

## 📝 ملاحظات هامة

### الأمان
- ✅ جميع البيانات محمية بقواعد Firestore
- ✅ المصادقة مطلوبة لجميع العمليات
- ✅ كل دور له صلاحيات محددة
- ✅ العلاقات محمية بقواعد التحقق

### الأداء
- ⚡ استخدام Next.js 14 App Router
- ⚡ تحميل البيانات بشكل متوازي
- ⚡ Caching مدمج
- ⚡ تحسين الصور تلقائياً

### قابلية التوسع
- 📈 يدعم آلاف المستخدمين
- 📈 Firestore قابل للتوسع تلقائياً
- 📈 يمكن إضافة مراكز متعددة
- 📈 سهولة إضافة ميزات جديدة

---

## 🐛 استكشاف الأخطاء

### مشاكل شائعة

#### خطأ في الاتصال بـ Firebase
```
تأكد من:
1. بيانات .env.local صحيحة
2. Firebase Authentication مفعّل
3. Firestore Database منشئ
```

#### خطأ في الصلاحيات
```
تأكد من:
1. رفع firestore.rules
2. المستخدم لديه الدور الصحيح
3. العلاقات محددة بشكل صحيح
```

#### خطأ في العلاقات
```
تأكد من:
1. الطالب مرتبط بولي أمر
2. الطالب مرتبط بمعلم واحد على الأقل
3. IDs صحيحة في قاعدة البيانات
```

---

## 📞 الدعم

للمساعدة والدعم:
- افتح Issue على GitHub
- راجع التوثيق
- تحقق من Firebase Documentation

---

## 📄 الترخيص

هذا المشروع مرخص تحت MIT License.

---

## 👥 المساهمة

المساهمات مرحب بها! يرجى:
1. Fork المشروع
2. إنشاء branch جديد
3. عمل commit للتغييرات
4. Push إلى Branch
5. فتح Pull Request

---

## 🎯 خارطة الطريق

### المميزات المستقبلية
- [ ] نظام الإشعارات الفورية
- [ ] دردشة بين المعلمين وأولياء الأمور
- [ ] تقارير PDF قابلة للطباعة
- [ ] تطبيق جوال (React Native)
- [ ] نظام الدفع الإلكتروني
- [ ] لوحة تحليلات متقدمة
- [ ] تكامل مع أنظمة خارجية
- [ ] دعم لغات متعددة

---

## 🙏 شكر وتقدير

- Next.js Team
- Firebase Team
- Tailwind CSS Team
- Open Source Community

---

**بُني بـ ❤️ للمجتمع التعليمي العربي**
